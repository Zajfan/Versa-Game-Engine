#ifndef GRAPHEXECUTOR_H
#define GRAPHEXECUTOR_H

#include "Graph.h"

class GraphExecutor
{
public:
    static void Execute(Graph& graph);

private:
    // ... (Helper functions for topological sorting, data flow, event handling, etc.)
};

#endif // GRAPHEXECUTOR_H#pragma once
